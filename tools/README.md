# 工具说明

- `render_submission_pdf.py` 用于生成中文申报书 PDF，输出路径为 `docs/submission-application.pdf`。

在任意工作目录中，可以使用安装了 ReportLab 的 Python 环境运行：

```bash
python tools/render_submission_pdf.py
```

生成的 PDF 使用参赛者石子硕的公开身份、仓库和包信息。
