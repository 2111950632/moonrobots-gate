# 更新日志

## 0.2.0

- 将原型扩展为模块化的 Robots 协议策略工程工具。
- 增加防御式解析限制、源指令保存、稳定诊断代码和扩展指令保存。
- 增加面向 RFC 的同等匹配组合并、百分号编码归一化、`/robots.txt` 隐式访问、抓取延迟和决策轨迹。
- 增加 URL 解析、语义检查、规范化渲染、访问矩阵、结构差异和行为差异。
- 增加 XML 与纯文本站点地图解析、元数据校验、跨域检查和被阻止条目报告。
- 增加确定性抓取规划，并支持协议、请求数、字节数、深度、单主机配额、延迟和去重约束。
- 增加 Markdown 和 JSON 聚合审计报告。
- 将 CLI 扩展到九组命令，并增加站点地图和抓取规划示例。
- 将自动化测试扩展到 102 项。

## 0.1.0

- 实现初始 `robots.txt` 解析器和允许、拒绝判断引擎。
- 增加通配符和末尾锚点路径匹配。
- 增加带命中行号的可解释决策。
- 增加 CLI、示例、测试、CI 和申报文档。
